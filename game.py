"""
this file will need to be refactored
"""



gameOver = False
winState = False
drawMap
drawSnake
drawCookie

while not gameOver:

    #the game is over if the snake head touches any of it's body
    if snake.isTouchingBody == True:     
        gameOver = True:
        take input
            calculate next draw
                print input on screen
                    check if snake kills it self(gameOver) or picks up cookie
                        check if last cookie, if it is gameOver and player Wins
                        if not last cookie
                            end this current loop


